[phases.setup]
# Install Node 20 + Python 3 from nixpkgs
nixPkgs = ["nodejs_20", "python312", "python312Packages.pip"]

[phases.install]
cmds = [
  "pip install yt-dlp",
  "npm ci"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
